pipeline {
    agent none
    stages {
        stage('mellon') {
            steps {
                echo sh(script: 'env|sort', returnStdout: true)
                echo ' hola !!!'
            }
        }
    }
}
