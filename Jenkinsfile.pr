pipeline {
    agent { label 'jenkins-slave-npm' }
    stages {
        stage('mellon') {
            steps {
                echo sh(script: 'env|sort', returnStdout: true)
                echo ' hola !!!'
            }
        }
    }
}
